---
kind: procedure
scope: dispatch
audience: coder
authority: method-only
---

# Coder Implementation

Use only after explicit `IMPLEMENTATION_AUTHORIZED`.

1. Confirm the Task, authorization delta, subject, lease epoch, and worktree still match.
2. Implement the smallest coherent behavior inside exact write scope.
3. Preserve frozen decisions, public boundaries, and unrelated work.
4. Add or update focused evidence for behavioral changes; use deterministic checks for mechanical work.
5. Run focused checks before broader regression checks.
6. If another path, Contract, dependency, or external action becomes necessary, use the loaded scope-escalation procedure and stop.
7. Do not schedule independent testing or review.

The Task and authorization delta govern exact files, semantics, verification, Report path, and commit message. This procedure supplies method only.
