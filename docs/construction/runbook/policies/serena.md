# Serena Policy — Compatibility Entry Point

This path remains for historical references. New leases and dispatches load exact split Bricks:

```text
lease:    docs/construction/runbook/policies/serena-safety.md
dispatch: docs/construction/runbook/policies/serena-operations.md
```

Do not load both automatically. The Orchestrator selects the operations Brick only when Serena is useful for the current dispatch.
