# AI_block Project Profile — Compatibility Entry Point

This path remains for historical Tasks and Reports. New context composition uses exact split Bricks:

```text
Orchestrator:
  docs/construction/runbook/project/orchestrator-profile.md

Worker lease:
  docs/construction/runbook/project/worker-lease-policy.md
```

Do not load both automatically. The Orchestrator profile is not default Worker context.
